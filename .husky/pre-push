#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running build checks..."

# Build frontend
cd frontend
bun run build || exit 1

# Build backend (when you add a build script)
# cd ../backend
# bun run build || exit 1

echo "✅ Build checks passed"
